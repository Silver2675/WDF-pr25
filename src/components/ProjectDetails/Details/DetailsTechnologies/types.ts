import { BasicInformation } from "../../types";

export interface ChipData {
    key: number;
    label: string;
}

export interface DetailsTechnologiesProps {
    information: BasicInformation
    label: string
}